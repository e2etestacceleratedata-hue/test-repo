name: 'dbt_design_e2e'
version: '1.0.0'
config-version: 2

profile: 'dbt_fab_spark'

model-paths: ['models']
analysis-paths: ['analyses']
test-paths: ['tests']
seed-paths: ['seeds']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  dbt_design_e2e:
    +materialized: table
